*---------------git--------------*

*-----------------configuring git---------------------*

git config --global user.name "My Name"
git config -global user.email "user@email234.com"
git config --list

*-------------------Clone & status----------------------------*

Clone -> cloning a repositry on your local machine 
            git clone <-some link->
Status -> display the status of the code 
            git status

            we have four types of status 
              1. untracked -> new file that git doesn't track yet
              2. modified 
